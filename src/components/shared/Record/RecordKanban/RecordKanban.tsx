import React from "react";
import { ADDITIONAL_PROP } from "../../../../types/application.type";

export const RecordKanban = ({ emoji, title, data }: { emoji: string; title: string; data: ADDITIONAL_PROP }) => {
  return <div>RecordKanban</div>;
};
