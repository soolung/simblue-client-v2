export type APPLICATION = {
  allowsDuplication: boolean;
  description: string;
  emoji: string;
  endDate: string;
  id: number;
  replyId: number | null;
  startDate: string;
  status: string;
  title: string;
};
